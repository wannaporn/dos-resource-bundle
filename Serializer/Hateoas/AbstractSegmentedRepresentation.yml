Hateoas\Representation\AbstractSegmentedRepresentation:
    exclusion_policy: all
    properties:
        limit:
            type: integer
            expose: true
            groups: [Default, Hateoas]
        total:
            type: integer
            expose: true
            groups: [Default, Hateoas]
